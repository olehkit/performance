package tests;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.github.javafaker.Faker;
import configuration.HTTPClientSM;
import org.testng.annotations.Test;

import java.text.SimpleDateFormat;
import java.util.Date;

public class CreateUserTest {

    Faker faker = new Faker();

    @Test
    public void test01() throws JsonProcessingException, InterruptedException {
        HTTPClientSM.createCustomer(new SimpleDateFormat("yyyyMMddHHmmss").format(new Date()) + "@guerrillamail.net", faker.name().firstName(), faker.name().lastName());
    }

    @Test
    public void test02() throws JsonProcessingException, InterruptedException {
        HTTPClientSM.createCustomer(new SimpleDateFormat("yyyyMMddHHmmss").format(new Date()) + "@guerrillamail.net", faker.name().firstName(), faker.name().lastName());
    }

    @Test
    public void test03() throws JsonProcessingException, InterruptedException {
        HTTPClientSM.createCustomer(new SimpleDateFormat("yyyyMMddHHmmss").format(new Date()) + "@guerrillamail.net", faker.name().firstName(), faker.name().lastName());
    }

    @Test
    public void test04() throws JsonProcessingException, InterruptedException {
        HTTPClientSM.createCustomer(new SimpleDateFormat("yyyyMMddHHmmss").format(new Date()) + "@guerrillamail.net", faker.name().firstName(), faker.name().lastName());
    }

    @Test
    public void test05() throws JsonProcessingException, InterruptedException {
        HTTPClientSM.createCustomer(new SimpleDateFormat("yyyyMMddHHmmss").format(new Date()) + "@guerrillamail.net", faker.name().firstName(), faker.name().lastName());
    }

    @Test
    public void test06() throws JsonProcessingException, InterruptedException {
        HTTPClientSM.createCustomer(new SimpleDateFormat("yyyyMMddHHmmss").format(new Date()) + "@guerrillamail.net", faker.name().firstName(), faker.name().lastName());
    }

    @Test
    public void test07() throws JsonProcessingException, InterruptedException {
        HTTPClientSM.createCustomer(new SimpleDateFormat("yyyyMMddHHmmss").format(new Date()) + "@guerrillamail.net", faker.name().firstName(), faker.name().lastName());
    }

    @Test
    public void test08() throws JsonProcessingException, InterruptedException {
        HTTPClientSM.createCustomer(new SimpleDateFormat("yyyyMMddHHmmss").format(new Date()) + "@guerrillamail.net", faker.name().firstName(), faker.name().lastName());
    }

    @Test
    public void test09() throws JsonProcessingException, InterruptedException {
        HTTPClientSM.createCustomer(new SimpleDateFormat("yyyyMMddHHmmss").format(new Date()) + "@guerrillamail.net", faker.name().firstName(), faker.name().lastName());
    }

    @Test
    public void test10() throws JsonProcessingException, InterruptedException {
        HTTPClientSM.createCustomer(new SimpleDateFormat("yyyyMMddHHmmss").format(new Date()) + "@guerrillamail.net", faker.name().firstName(), faker.name().lastName());
    }

    @Test
    public void test11() throws JsonProcessingException, InterruptedException {
        HTTPClientSM.createCustomer(new SimpleDateFormat("yyyyMMddHHmmss").format(new Date()) + "@guerrillamail.net", faker.name().firstName(), faker.name().lastName());
    }

    @Test
    public void test12() throws JsonProcessingException, InterruptedException {
        HTTPClientSM.createCustomer(new SimpleDateFormat("yyyyMMddHHmmss").format(new Date()) + "@guerrillamail.net", faker.name().firstName(), faker.name().lastName());
    }

    @Test
    public void test13() throws JsonProcessingException, InterruptedException {
        HTTPClientSM.createCustomer(new SimpleDateFormat("yyyyMMddHHmmss").format(new Date()) + "@guerrillamail.net", faker.name().firstName(), faker.name().lastName());
    }

    @Test
    public void test14() throws JsonProcessingException, InterruptedException {
        HTTPClientSM.createCustomer(new SimpleDateFormat("yyyyMMddHHmmss").format(new Date()) + "@guerrillamail.net", faker.name().firstName(), faker.name().lastName());
    }

    @Test
    public void test15() throws JsonProcessingException, InterruptedException {
        HTTPClientSM.createCustomer(new SimpleDateFormat("yyyyMMddHHmmss").format(new Date()) + "@guerrillamail.net", faker.name().firstName(), faker.name().lastName());
    }

    @Test
    public void test16() throws JsonProcessingException, InterruptedException {
        HTTPClientSM.createCustomer(new SimpleDateFormat("yyyyMMddHHmmss").format(new Date()) + "@guerrillamail.net", faker.name().firstName(), faker.name().lastName());
    }

    @Test
    public void test17() throws JsonProcessingException, InterruptedException {
        HTTPClientSM.createCustomer(new SimpleDateFormat("yyyyMMddHHmmss").format(new Date()) + "@guerrillamail.net", faker.name().firstName(), faker.name().lastName());
    }

    @Test
    public void test18() throws JsonProcessingException, InterruptedException {
        HTTPClientSM.createCustomer(new SimpleDateFormat("yyyyMMddHHmmss").format(new Date()) + "@guerrillamail.net", faker.name().firstName(), faker.name().lastName());
    }

    @Test
    public void test19() throws JsonProcessingException, InterruptedException {
        HTTPClientSM.createCustomer(new SimpleDateFormat("yyyyMMddHHmmss").format(new Date()) + "@guerrillamail.net", faker.name().firstName(), faker.name().lastName());
    }

    @Test
    public void test20() throws JsonProcessingException, InterruptedException {
        HTTPClientSM.createCustomer(new SimpleDateFormat("yyyyMMddHHmmss").format(new Date()) + "@guerrillamail.net", faker.name().firstName(), faker.name().lastName());
    }

}
